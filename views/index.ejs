<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Template</title>
</head>
<body>
  <h1>Index</h1>
  
  <% if (user) { %>
    <h2><%= user.displayName %></h2>
    <img src='<%= user.photos[0].value %>' width='60'/>
    <a href="/logout">Log out</a>
  <% } else { %>
    <a href="/oauth">Login with Google</a>
  <% } %>

</body>
</html>